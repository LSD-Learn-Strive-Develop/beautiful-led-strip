# Core dependencies
aiogram>=3.0
aiohttp-socks>=0.9
requests>=2.28

# Hardware (Raspberry Pi only)
# Install these on the Pi: board, neopixel
# They require specific system libraries

# Configuration
python-dotenv>=1.0
